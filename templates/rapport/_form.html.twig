{{ form_start(form) }}
{{ form_row(form.date) }}
{{ form_row(form.motif) }}
{{ form_row(form.bilan) }}
{{ form_row(form.idmedecin) }}
<div id="offrir_list" data-prototype="{{ form_widget(form.offrirs.vars.prototype)|e('html_attr') }}">
    {% for row in form.offrirs %}
        <div class="panel panel-warning">
            <div class="panel-heading">
                <strong>{{ row.vars.value.medicament.nomcommercial }}</strong>
            </div>
                
            <div class="ui-widget">
                <div class="panel-body" id="test">
                    {{ form_row(row) }}
                </div>
            </div>
        </div>
    {% else %}
        {{ form_row(form.offrirs) }}
    {% endfor %}
</div>
<br><br>
<center><button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button></center>

{{ form_end(form) }}








{#<div class="js-medicament" data-listemedicaments="{{ lesMedicaments|join(',') }}"> 
    <script>
        $('document').ready(function() {

            $( "#ajouterEchantillon" ).click(function() {

                var lesmedicaments = $('.js-medicament').data('listemedicaments');

                var lesmedicaments = lesmedicaments.split(",");

                console.log(lesmedicaments);
                
                $( ".autocomplete" ).autocomplete({
                    source: lesmedicaments
                });
            });
        });
    </script>
</div>#}
{# render each row and field in the embedded form, in this example it's rendering the experience (Exp) fiels as rows #}

